---
interface Props {
  title: string;
  subtitle?: string;
}

const { title, subtitle } = Astro.props;
---

<header class="gallery-header reveal">
  <h1>{title}</h1>
  {subtitle && <p>{subtitle}</p>}
</header>
