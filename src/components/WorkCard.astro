---
interface Props {
  title: string;
  author: string;
  src: string;
}

const { title, author, src } = Astro.props;
---

<article class="gallery-item reveal">
  <img src={src} alt={title} loading="lazy" />
  <h2>{title}</h2>
  <p>{author}</p>
</article>
